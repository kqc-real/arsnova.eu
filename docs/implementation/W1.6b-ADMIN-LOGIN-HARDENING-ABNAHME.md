# W1.6b-Abnahme: Progressive Admin-Login-Härtung

**Stand:** 2026-07-24  
**Owner:** Backend / Security  
**Ticket:** PR-Slice W1.6b  
**Abhängigkeiten:** Redis, W1.6a-Telemetrie  
**Rollback:** Code-Revert oder kontrollierte Anpassung der Rate-Limit-Env

## Schutzmodell

- Jeder `admin.login`-Versuch benötigt vor dem Secret-Vergleich ein atomar
  gebuchtes globales Redis-Permit. Ohne Permit wird der Kandidat nicht geprüft.
  Das schützt das einzelne globale Shared Secret auch gegen IP-verteilte
  Angriffe.
- Die Verzögerung steigt exponentiell von standardmäßig 100 ms auf höchstens
  2000 ms. Das Fehlbudget erlaubt standardmäßig 60 Versuche pro 60 Sekunden.
- Maximal 25 verzögerte Requests warten gleichzeitig pro Backend-Prozess.
  Weitere ungültige Versuche erhalten sofort `TOO_MANY_REQUESTS`.
- Nach ausgeschöpftem Globalbudget liefert der Server eine positive
  `retryAfterSeconds`-Angabe über den bestehenden tRPC-Error-Transport.
- Das Admin-Frontend lokalisiert die Drosselungsmeldung und zeigt die konkrete
  Wartezeit in allen fünf gepflegten Sprachen.
- Das Pre-Auth-Budget gilt auch für gültige Zugangsdaten: Ein verteilter Angriff
  kann den Admin-Login daher bis zum Ablauf des höchstens 60 Sekunden langen
  Fensters vorübergehend sperren. Garantierte Verfügbarkeit unter Angriff
  erfordert ein vorab vertrauenswürdiges Merkmal wie VPN, mTLS oder eine
  Netzfreigabe und ist nicht Teil dieses Slices.
- Es gibt bewusst weder einen Admin-IP-Key noch einen IP-Lockout. Damit kann
  ein Angreifer keine gemeinsam genutzte Hochschul-/Betreiber-IP sperren.
- Alle fehlgeschlagenen beziehungsweise vorab gedrosselten Admin-Logins sind in
  `health.securityStats.adminLoginFailuresLastMinute` sichtbar. 429 werden
  zusätzlich unter der Kategorie `adminLogin` und in den gesampelten
  `rate_limit_429`-Logs erfasst; ungültige geprüfte Kandidaten erscheinen
  gesampelt als `admin_login_failed`, ohne Secret oder IP.

## Abnahmekriterien

- [x] Ohne atomar gebuchtes Pre-Auth-Permit findet kein Secret-Vergleich statt.
- [x] Wiederholte Fehlversuche erhalten einen progressiv steigenden Delay.
- [x] Globalbudget und wartende Requests sind hart begrenzt.
- [x] Der mögliche kurze globale Lockout ist fachlich akzeptiert und dokumentiert.
- [x] Es entsteht kein IP-basierter Lockout.
- [x] 429 enthalten eine positive Wartezeit und sind als `adminLogin` messbar.
- [x] Die Admin-UI zeigt lokalisierte Rate-Limit-Meldung und Wartezeit.
- [x] Env-Beispiele, Admin-Flow und Monitoring-Runbook sind aktualisiert.

## Testabdeckung

```bash
npm run test -w @arsnova/backend -- --run \
  src/lib/adminLoginProtection.test.ts \
  src/__tests__/admin.test.ts \
  src/lib/abuseTelemetry.test.ts \
  src/__tests__/health.test.ts
(
  cd apps/backend
  RUN_REDIS_ADMIN_LOGIN_TESTS=1 \
  RATE_LIMIT_ADMIN_LOGIN_WINDOW_SECONDS=30 \
  RATE_LIMIT_ADMIN_LOGIN_GLOBAL_ATTEMPTS_PER_WINDOW=5 \
  RATE_LIMIT_ADMIN_LOGIN_DELAY_BASE_MS=10 \
  RATE_LIMIT_ADMIN_LOGIN_DELAY_MAX_MS=80 \
    npx vitest run src/lib/adminLoginProtection.redis.test.ts
)
npm run test -w @arsnova/frontend -- --run \
  src/app/core/localize-known-server-message.spec.ts
npm run build:localize -w @arsnova/frontend
npm run typecheck
npm run lint
```
